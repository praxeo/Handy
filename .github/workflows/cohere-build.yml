name: "Cohere Build"
on:
  workflow_dispatch:

jobs:
  build-windows:
    uses: ./.github/workflows/build.yml
    with:
      platform: windows-latest
      target: x86_64-pc-windows-msvc
      ref: cohere-handy
